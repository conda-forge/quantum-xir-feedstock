{% set name = "quantum-xir" %}
{% set version = "0.2.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/XanaduAI/xir/archive/refs/tags/v{{ version }}.tar.gz
  sha256: 16b75c5bf37cee170c6a3309386def86f23ba527638a3493a1a3db5f1c61107e

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - lark-parser >=0.11.0
    - python >=3.6

test:
  imports:
    - xir
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/XanaduAI/xir
  summary: XIR is an intermediate representation (IR) for quantum circuits.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - BastianZim
